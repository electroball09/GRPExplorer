@if (Points != null)
{
    @foreach (var point in Points.Points)
    {
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
        <MudText Typo="Typo.body2">@point.ToString()</MudText>
    }
}


@code {
    [Parameter]
    public BasicPointRenderer? Points { get; set; } 

    public void Update()
    {
        //StateHasChanged();
    }
}
